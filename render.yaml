services:
  - type: web
    name: infinity-browser-ai
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:server
    envVars:
      - key: ADMIN_API_KEY
        value: change-me
      - key: PORT
        value: 10000
